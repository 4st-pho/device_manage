import 'package:flutter/material.dart';
import 'app_size.dart';

class AppFont {
  static const blueText = TextStyle(
    color: Colors.blue,
    fontSize: TextSize.medium,
  );
  static const blueTitle = TextStyle(
    color: Colors.blue,
    fontSize: TextSize.large,
  );
  static const whiteSubtitle = TextStyle(
    color: Colors.white,
    fontSize: TextSize.small,
  );
  static const whiteText = TextStyle(
    color: Colors.white,
    fontSize: TextSize.medium,
    height: 1.5,
  );
  static const whiteTitle = TextStyle(
    color: Colors.white,
    fontSize: TextSize.large,
  );
  static const whiteHeadline = TextStyle(
    color: Colors.white,
    fontSize: TextSize.verylarge,
  );
}
